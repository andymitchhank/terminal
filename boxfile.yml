run.config:
  engine: python
  engine.config:
    runtime: python-3.6
  extra_packages:
    - nginx

web.main:
  start:
    nginx: nginx -c /app/etc/nginx.conf
    flask: gunicorn -c /app/etc/gunicorn.py app:app --chdir /app/src
  # writable_dirs:
  #   - tmp

data.db:
  image: nanobox/postgresql:9.5

# worker.main:
#   start: 'python jobs-worker.py'

